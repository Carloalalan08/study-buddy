import { getQuote, getDefinition } from './js/modules/api.js';

document.addEventListener('DOMContentLoaded', () => {
  getQuote(); // Load quote on page load

  // other functions like loadToDos(), setupTimer(), etc. will go here
});
